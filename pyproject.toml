[project]
name = "bot-ai-cloudflare"
version = "0.1.0"
description = "Telegram sports analytics bot on Cloudflare Python Workers"
requires-python = ">=3.13"
dependencies = [
  "httpx",
]

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
